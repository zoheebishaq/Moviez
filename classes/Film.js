import { Media } from "./Media";


export class Film extends Media{

    constructor(unTitre, unPoster, imbdID, uneAnnée, unType)
    {
        super(unTitre, unPoster, imbdID, uneAnnée, unType);
    }
   //TODO faire fetchJSON
}